/**
 * Turbo CPS for boot splash + mission banner typing during `runBootRender`.
 * Single source of truth for Node (`game.mjs`) and browser (`web/campaign-browser.mjs`).
 */
export const BOOT_RENDER_CPS = 80000;
